[package]
name = "vita-log"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

# M15-A3: dropped tracing + tracing-subscriber + crossbeam-channel +
# parking_lot. Those use pthread state that the SUPRX can't init.
# Replaced with our own macros + vita-thread + vita-sync.
[dependencies]
vita-thread = { path = "../vita-thread" }
vita-sync   = { path = "../vita-sync" }
time        = { workspace = true }
thiserror   = { workspace = true }

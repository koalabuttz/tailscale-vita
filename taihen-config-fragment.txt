# tailscale-vita-plugin — taiHEN config fragment
#
# Copy this stanza into ur0:tai/config.txt (taiHEN/Ensō's DEFAULT config
# partition — note ur0:, NOT ux0:; the standard plugins live under ur0:tai/
# too). Append after any existing *KERNEL / *main sections. The plugin loads
# ONLY when the demo app
# (TVIT00010) launches — staging here intentionally instead of *main
# so a plugin crash during bringup tears down the demo, not SceShell.
# (Memory note m11_suprx_loader_findings: SceShell brick risk is the
# reason the earlier plugin attempt was pulled. *<TITLEID> staging
# avoids it.)
#
# After editing config.txt, reboot the Vita (taiHEN re-reads config
# at boot). For subsequent .suprx updates, FTP-replace the file in
# ur0:tai/ (the path in the stanza below) and reboot — taiHEN doesn't
# hot-reload. Verify the on-device size matches your fresh local build;
# a stale size means you pushed to the wrong drive (e.g. ux0:tai/).

*TVIT00010
ur0:tai/tailscale-vita-plugin.suprx
